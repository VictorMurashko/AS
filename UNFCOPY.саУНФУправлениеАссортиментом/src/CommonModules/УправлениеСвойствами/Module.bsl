
&Вместо("НаборСвойствПоИмени")
Функция саУНФ_НаборСвойствПоИмени(ИмяНабора)
	Если  ИмяНабора = "Документ_саУНФ_ЗаказСнабжения" Тогда
	          ИмяНабора="Документ_ЗаказПокупателя";
	КонецЕсли; 
		
	Результат = ПродолжитьВызов(ИмяНабора);
	Возврат Результат;
КонецФункции
